// WXS 工具：WXML 表达式不支持 JS 方法调用，需要的判断放这里。
function inArray(arr, val) {
  if (!arr) return false;
  for (var i = 0; i < arr.length; i++) {
    if (arr[i] === val) return true;
  }
  return false;
}

module.exports = {
  inArray: inArray,
};
